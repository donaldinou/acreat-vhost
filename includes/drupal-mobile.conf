<If "${DrupalPath}/mobile -d">
    <IfModule mod_rewrite.c>
        RewriteRule ^/sites/${ServerName}/mobile(.*) /mobile$1 [R=permanent,L]
    </IfModule>
    Alias /mobile ${DrupalPath}/mobile/
</If>
