<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.${ServerName}$ [NC]
    RewriteRule ^(.*)$ ${Scheme}://www.${ServerName}$1 [R=permanent,L]
    IncludeOptional includes/*-${ServerName}.conf
</IfModule>
