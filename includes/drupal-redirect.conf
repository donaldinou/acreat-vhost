<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^{$ServerName}$ [NC]
    RewriteRule ^(.*)$ ${Scheme}://${ServerName}$1 [R=permanent,L]
    IncludeOptional includes/*${ServerName}.conf
</IfModule>
