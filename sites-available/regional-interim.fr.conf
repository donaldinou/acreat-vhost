<VirtualHost *:80 *:443>
    Include includes/undefine-vars.conf

    Define ServerName regional-interim.fr
    Define RootPath ${DRUPAL_7_HOME}
    Define SSLActive

    Redirect 301 /sites/regional-interim.fr/test/ http://www.regional-interim.fr.acreat.net/sites/regional-interim.fr/test/

    Include includes/define-server.conf
    Include includes/drupal-website.conf
    Include includes/drupal-https.conf
    Include includes/define-redirect-www.conf

    # Not working
    #<ifModule mod_headers.c>
    #	SetEnvIf Request_URI "/sites/${ServerName}/files/webform/" WEBFORM_HEADER
    #	Header set X-Robots-Tag "noindex, nofollow" env=WEBFORM_HEADER
    #</ifModule>
    <Directory /${RootPath}/sites/${ServerName}/files/webform/>
      Header Set X-Robots-Tag "noindex, nofollow"
    </Directory>

    Include includes/define-logs.conf

    ServerAlias regionalinterim.fr *.regionalinterim.fr
</VirtualHost>
