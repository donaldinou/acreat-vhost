#
# This is the main Apache HTTP server configuration file.  It contains the
# configuration directives that give the server its instructions.
# See <URL:http://httpd.apache.org/docs/2.4/> for detailed information.
# In particular, see 
# <URL:http://httpd.apache.org/docs/2.4/mod/directives.html>
# for a discussion of each configuration directive.
#
# Do NOT simply read the instructions in here without understanding
# what they do.  They're here only as hints or reminders.  If you are unsure
# consult the online docs. You have been warned.  
#
# Configuration and logfile names: If the filenames you specify for many
# of the server's control files begin with "/" (or "drive:/" for Win32), the
# server will use that explicit path.  If the filenames do *not* begin
# with "/", the value of ServerRoot is prepended -- so 'log/access_log'
# with ServerRoot set to '/www' will be interpreted by the
# server as '/www/log/access_log', where as '/log/access_log' will be
# interpreted as '/log/access_log'.

#
# ServerRoot: The top of the directory tree under which the server's
# configuration, error, and log files are kept.
#
# Do not add a slash at the end of the directory path.  If you point
# ServerRoot at a non-local disk, be sure to specify a local disk on the
# Mutex directive, if file-based mutexes are used.  If you wish to share the
# same ServerRoot for multiple httpd daemons, you will need to change at
# least PidFile.
#
ServerRoot "/etc/httpd"

#
# Listen: Allows you to bind Apache to specific IP addresses and/or
# ports, instead of the default. See also the <VirtualHost>
# directive.
#
# Change this to Listen on specific IP addresses as shown below to 
# prevent Apache from glomming onto all bound IP addresses.
#
#Listen 12.34.56.78:80
Listen 80

#
# Dynamic Shared Object (DSO) Support
#
# To be able to use the functionality of a module which was built as a DSO you
# have to place corresponding `LoadModule' lines at this location so the
# directives contained in it are actually available _before_ they are used.
# Statically compiled modules (those listed by `httpd -l') do not need
# to be loaded here.
#
# Example:
# LoadModule foo_module modules/mod_foo.so
#
Include conf.modules.d/*.conf

#
# If you wish httpd to run as a different user or group, you must run
# httpd as root initially and it will switch.  
#
# User/Group: The name (or #number) of the user/group to run httpd as.
# It is usually good practice to create a dedicated user and group for
# running httpd, as with most system services.
#
User apache
Group apache

# 'Main' server configuration
#
# The directives in this section set up the values used by the 'main'
# server, which responds to any requests that aren't handled by a
# <VirtualHost> definition.  These values also provide defaults for
# any <VirtualHost> containers you may define later in the file.
#
# All of these directives may appear inside <VirtualHost> containers,
# in which case these default settings will be overridden for the
# virtual host being defined.
#

#
# ServerAdmin: Your address, where problems with the server should be
# e-mailed.  This address appears on some server-generated pages, such
# as error documents.  e.g. admin@your-domain.com
#
ServerAdmin root@localhost

#
# ServerName gives the name and port that the server uses to identify itself.
# This can often be determined automatically, but we recommend you specify
# it explicitly to prevent problems during startup.
#
# If your host doesn't have a registered DNS name, enter its IP address here.
#
#ServerName www.example.com:80

#
# Deny access to the entirety of your server's filesystem. You must
# explicitly permit access to web content directories in other 
# <Directory> blocks below.
#
<Directory />
    AllowOverride none
    Require all denied
</Directory>

#
# Note that from this point forward you must specifically allow
# particular features to be enabled - so if something's not working as
# you might expect, make sure that you have specifically enabled it
# below.
#

#
# DocumentRoot: The directory out of which you will serve your
# documents. By default, all requests are taken from this directory, but
# symbolic links and aliases may be used to point to other locations.
#
DocumentRoot "/var/www/html"

#
# Relax access to content within /var/www.
#
<Directory "/var/www">
    AllowOverride None
    # Allow open access:
    Require all granted
</Directory>

# Further relax access to the default document root:
<Directory "/var/www/html">
    #
    # Possible values for the Options directive are "None", "All",
    # or any combination of:
    #   Indexes Includes FollowSymLinks SymLinksifOwnerMatch ExecCGI MultiViews
    #
    # Note that "MultiViews" must be named *explicitly* --- "Options All"
    # doesn't give it to you.
    #
    # The Options directive is both complicated and important.  Please see
    # http://httpd.apache.org/docs/2.4/mod/core.html#options
    # for more information.
    #
    Options Indexes FollowSymLinks

    #
    # AllowOverride controls what directives may be placed in .htaccess files.
    # It can be "All", "None", or any combination of the keywords:
    #   Options FileInfo AuthConfig Limit
    #
    AllowOverride None

    #
    # Controls who can get stuff from this server.
    #
    Require all granted
</Directory>

#
# DirectoryIndex: sets the file that Apache will serve if a directory
# is requested.
#
<IfModule dir_module>
    DirectoryIndex index.html
</IfModule>

#
# The following lines prevent .htaccess and .htpasswd files from being 
# viewed by Web clients. 
#
<Files ".ht*">
    Require all denied
</Files>

#
# ErrorLog: The location of the error log file.
# If you do not specify an ErrorLog directive within a <VirtualHost>
# container, error messages relating to that virtual host will be
# logged here.  If you *do* define an error logfile for a <VirtualHost>
# container, that host's errors will be logged there and not here.
#
ErrorLog "logs/error_log"

#
# LogLevel: Control the number of messages logged to the error_log.
# Possible values include: debug, info, notice, warn, error, crit,
# alert, emerg.
#
LogLevel warn

<IfModule log_config_module>
    #
    # The following directives define some format nicknames for use with
    # a CustomLog directive (see below).
    #
    LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined
    LogFormat "%h %l %u %t \"%r\" %>s %b" common

    <IfModule logio_module>
      # You need to enable mod_logio.c to use %I and %O
      LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %I %O" combinedio
    </IfModule>

    #
    # The location and format of the access logfile (Common Logfile Format).
    # If you do not define any access logfiles within a <VirtualHost>
    # container, they will be logged here.  Contrariwise, if you *do*
    # define per-<VirtualHost> access logfiles, transactions will be
    # logged therein and *not* in this file.
    #
    #CustomLog "logs/access_log" common

    #
    # If you prefer a logfile with access, agent, and referer information
    # (Combined Logfile Format) you can use the following directive.
    #
    CustomLog "logs/access_log" combined
</IfModule>

<IfModule alias_module>
    #
    # Redirect: Allows you to tell clients about documents that used to 
    # exist in your server's namespace, but do not anymore. The client 
    # will make a new request for the document at its new location.
    # Example:
    # Redirect permanent /foo http://www.example.com/bar

    #
    # Alias: Maps web paths into filesystem paths and is used to
    # access content that does not live under the DocumentRoot.
    # Example:
    # Alias /webpath /full/filesystem/path
    #
    # If you include a trailing / on /webpath then the server will
    # require it to be present in the URL.  You will also likely
    # need to provide a <Directory> section to allow access to
    # the filesystem path.

    #
    # ScriptAlias: This controls which directories contain server scripts. 
    # ScriptAliases are essentially the same as Aliases, except that
    # documents in the target directory are treated as applications and
    # run by the server when requested rather than as documents sent to the
    # client.  The same rules about trailing "/" apply to ScriptAlias
    # directives as to Alias.
    #
    ScriptAlias /cgi-bin/ "/var/www/cgi-bin/"

</IfModule>

#
# "/var/www/cgi-bin" should be changed to whatever your ScriptAliased
# CGI directory exists, if you have that configured.
#
<Directory "/var/www/cgi-bin">
    AllowOverride None
    Options None
    Require all granted
</Directory>

<IfModule mime_module>
    #
    # TypesConfig points to the file containing the list of mappings from
    # filename extension to MIME-type.
    #
    TypesConfig /etc/mime.types

    #
    # AddType allows you to add to or override the MIME configuration
    # file specified in TypesConfig for specific file types.
    #
    #AddType application/x-gzip .tgz
    #
    # AddEncoding allows you to have certain browsers uncompress
    # information on the fly. Note: Not all browsers support this.
    #
    #AddEncoding x-compress .Z
    #AddEncoding x-gzip .gz .tgz
    #
    # If the AddEncoding directives above are commented-out, then you
    # probably should define those extensions to indicate media types:
    #
    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz

    #
    # AddHandler allows you to map certain file extensions to "handlers":
    # actions unrelated to filetype. These can be either built into the server
    # or added with the Action directive (see below)
    #
    # To use CGI scripts outside of ScriptAliased directories:
    # (You will also need to add "ExecCGI" to the "Options" directive.)
    #
    #AddHandler cgi-script .cgi

    # For type maps (negotiated resources):
    #AddHandler type-map var

    #
    # Filters allow you to process content before it is sent to the client.
    #
    # To parse .shtml files for server-side includes (SSI):
    # (You will also need to add "Includes" to the "Options" directive.)
    #
    AddType text/html .shtml
    AddOutputFilter INCLUDES .shtml
</IfModule>

#
# Specify a default charset for all content served; this enables
# interpretation of all content as UTF-8 by default.  To use the 
# default browser choice (ISO-8859-1), or to allow the META tags
# in HTML content to override this choice, comment out this
# directive:
#
AddDefaultCharset UTF-8

<IfModule mime_magic_module>
    #
    # The mod_mime_magic module allows the server to use various hints from the
    # contents of the file itself to determine its type.  The MIMEMagicFile
    # directive tells the module where the hint definitions are located.
    #
    MIMEMagicFile conf/magic
</IfModule>

#
# Customizable error responses come in three flavors:
# 1) plain text 2) local redirects 3) external redirects
#
# Some examples:
#ErrorDocument 500 "The server made a boo boo."
#ErrorDocument 404 /missing.html
#ErrorDocument 404 "/cgi-bin/missing_handler.pl"
#ErrorDocument 402 http://www.example.com/subscription_info.html
#

#
# EnableMMAP and EnableSendfile: On systems that support it, 
# memory-mapping or the sendfile syscall may be used to deliver
# files.  This usually improves server performance, but must
# be turned off when serving from networked-mounted 
# filesystems or if support for these functions is otherwise
# broken on your system.
# Defaults if commented: EnableMMAP On, EnableSendfile Off
#
#EnableMMAP off
EnableSendfile on

# Supplemental configuration
#
# Load config files in the "/etc/httpd/conf.d" directory, if any.
IncludeOptional conf.d/*.conf

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName evolis-avocats.fr
    ServerAlias *.evolis-avocats.fr
    ErrorLog logs/evolis-avocats.fr-error_log
    CustomLog logs/evolis-avocats.fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.evolis-avocats\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.evolis-avocats.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName rennesvolley35.fr
    ServerAlias *.rennesvolley35.fr
    ErrorLog logs/rennesvolley35.fr-error_log
    CustomLog logs/rennesvolley35.fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.rennesvolley35\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.rennesvolley35.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName destrudata.com
    ServerAlias *.destrudata.com
    ErrorLog logs/destrudata.com-error_log
    CustomLog logs/destrudata.com-access_log common

    RewriteEngine on
    #RewriteCond %{HTTP_HOST} !^www\.destrudata\.com$ [NC]
    #RewriteRule ^(.*)$ http://www.destrudata.com$1 [R=permanent,L]
    RewriteRule ^/sites/destrudata.com/mobile(.*) /mobile$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal710/sites/destrudata.com/mobile/
</VirtualHost>

<VirtualHost *:80>
        ServerName destrudata.fr
        ServerAlias www.destrudata.fr
        Redirect 301 / http://www.destrudata.com/
</VirtualHost>

<Directory /sites>
    Options Indexes FollowSymLinks
    AllowOverride All
    Order allow,deny
    Allow from all
</Directory>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName leroylogistique.com
    ServerAlias *.leroylogistique.com
    ErrorLog logs/leroylogistique.com-error_log
    CustomLog logs/leroylogistique.com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.leroylogistique\.com$ [NC]
    RewriteRule ^(.*)$ http://www.leroylogistique.com$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName defirh.fr
    ServerAlias *.defirh.fr
    ErrorLog logs/defirh-fr-error_log
    CustomLog logs/defirh-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.defirh\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.defirh.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerName defi-rh.fr
    ServerAlias www.defi-rh.fr
    Redirect 301 / http://www.defirh.fr/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName pommereul-demenagements.fr
    ServerAlias *.pommereul-demenagements.fr
    ErrorLog logs/pommereul-demenagements-fr-error_log
    CustomLog logs/pommereul-demenagements-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.pommereul-demenagements\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.pommereul-demenagements.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerName demenagements35.com
    ServerAlias www.demenagements35.com
    Redirect 301 / http://www.pommereul-demenagements.fr
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName lycee-descartes.eu
    ServerAlias *.lycee-descartes.eu
    ErrorLog logs/lycee-descartes-eu-error_log
    CustomLog logs/lycee-descartes-eu-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.lycee-descartes\.eu$ [NC]
    RewriteRule ^(.*)$ http://www.lycee-descartes.eu$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName globalnatura.eu
    ServerAlias *.globalnatura.eu
    ErrorLog logs/globalnatura-eu-error_log
    CustomLog logs/globalnatura-eu-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.globalnatura\.eu$ [NC]
    RewriteRule ^(.*)$ http://www.globalnatura.eu$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName centraleimmo.fr
    ServerAlias *.centraleimmo.fr
    ServerAlias *.la-centrale-immobiliere.fr la-centrale-immobiliere.fr
    ServerAlias www.rci69.fr *.rci69.fr
    ErrorLog logs/centraleimmo-fr-error_log
    CustomLog logs/centraleimmo-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} ^(www\.)?la-centrale-immobiliere.fr$
    RewriteRule ^(.*)$ http://www.centraleimmo.fr$1 [R=permanent,L]
    RewriteCond %{HTTP_HOST} ^(www\.)?rci69.fr$
    RewriteRule ^(.*)$ http://www.centraleimmo.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName agenceseineimmo.fr
    ServerAlias *.agenceseineimmo.fr
    ErrorLog logs/agenceseineimmo-fr-error_log
    CustomLog logs/agenceseineimmo-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.agenceseineimmo\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.agenceseineimmo.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName 4immo.fr
    ServerAlias *.4immo.fr
    ErrorLog logs/4immo-fr-error_log
    CustomLog logs/4immo-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.4immo\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.4immo.fr$1 [R=permanent,L]
    RewriteRule ^/sites/4immo.fr/mobile(.*) /mobile$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal710/sites/4immo.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName kampexport.com
    ServerAlias *.kampexport.com
    ErrorLog logs/kampexport-com-error_log
    CustomLog logs/kampexport-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.kampexport\.com$ [NC]
    RewriteRule ^(.*)$ http://www.kampexport.com$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName agenceduvieuxport.com
    ServerAlias *.agenceduvieuxport.com
    ErrorLog logs/agenceduvieuxport-com-error_log
    CustomLog logs/agenceduvieuxport-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} ^(www\.)?agenceduvieuxport.fr$
    RewriteRule ^(.*)$ http://www.agenceduvieuxport.com$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName gerinter.fr
    ServerAlias *.gerinter.fr
    ErrorLog logs/gerinter-fr-error_log
    CustomLog logs/gerinter-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.gerinter\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.gerinter.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName lecomtesyndic.fr
    ServerAlias *.lecomtesyndic.fr
    ErrorLog logs/lecomtesyndic-fr-error_log
    CustomLog logs/lecomtesyndic-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.lecomtesyndic\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.lecomtesyndic.fr$1 [R=permanent,L]
</VirtualHost>


<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName lgbtvoyages.com
    ServerAlias *.lgbtvoyages.com
    ErrorLog logs/lgbtvoyages-com-error_log
    CustomLog logs/lgbtvoyages-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.lgbtvoyages\.com$ [NC]
    RewriteRule ^(.*)$ http://www.lgbtvoyages.com$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName uflevage.fr
    ServerAlias *.uflevage.fr
    ErrorLog logs/uflevage-fr-error_log
    CustomLog logs/uflevage-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.uflevage\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.uflevage.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName lefeuvre-immobilier.com
    ServerAlias *.lefeuvre-immobilier.com
    ServerAlias www.cabinet-lefeuvre.com *.cabinet-lefeuvre.com
    ServerAlias lefeuvre-immobilier.eu *.lefeuvre-immobilier.eu
    ServerAlias nantes-immobilier.fr *.nantes-immobilier.fr
    ServerAlias lefeuvre-immobilier.fr *.lefeuvre-immobilier.fr
    ServerAlias cabinet-lefeuvre.fr *.cabinet-lefeuvre.fr
    ServerAlias lefeuvre-immobilier.net *.lefeuvre-immobilier.net
    ErrorLog logs/lefeuvre-immobilier-com-error_log
    CustomLog logs/lefeuvre-immobilier-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.lefeuvre-immobilier\.com$ [NC]
    RewriteRule ^(.*)$ http://www.lefeuvre-immobilier.com$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/lefeuvre-immobilier.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName thierry-immobilier.fr
    ServerAlias *.thierry-immobilier.fr
    ErrorLog logs/thierry-immobilier-fr-error_log
    CustomLog logs/thierry-immobilier-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.thierry-immobilier\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.thierry-immobilier.fr$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/thierry-immobilier.fr/mobile/
    Alias /newsletter ${DRUPAL_HOME}/drupal726/sites/thierry-immobilier.fr/newsletter/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName acreat.ca
    ServerAlias *.acreat.ca
    ErrorLog logs/acreat-ca-error_log
    CustomLog logs/acreat-ca-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.acreat\.ca$ [NC]
    RewriteRule ^(.*)$ http://www.acreat.ca$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName mgeffray-immobilier.fr
    ServerAlias *.mgeffray-immobilier.fr
    ErrorLog logs/mgeffray-immobilier-fr-error_log
    CustomLog logs/mgeffray-immobilier-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.mgeffray-immobilier\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.mgeffray-immobilier.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName salonespritmaison.com
    ServerAlias *.salonespritmaison.com
    ErrorLog logs/salonespritmaison-com-error_log
    CustomLog logs/salonespritmaison-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.salonespritmaison\.com$ [NC]
    RewriteRule ^(.*)$ http://www.salonespritmaison.com$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName mc2avocat.fr
    ServerAlias *.mc2avocat.fr
    ErrorLog logs/mc2avocat-fr-error_log
    CustomLog logs/mc2avocat-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.mc2avocat\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.mc2avocat.fr$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal710/
    ServerName pelatre-tp.com
    ServerAlias *.pelatre-tp.com
    ErrorLog logs/pelatre-tp-com-error_log
    CustomLog logs/pelatre-tp-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.pelatre-tp\.com$ [NC]
    RewriteRule ^(.*)$ http://www.pelatre-tp.com$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName bellecour-assurances.fr
    ServerAlias *.bellecour-assurances.fr *.bellecour.com
    ErrorLog logs/bellecour-assurances-fr-error_log
    CustomLog logs/bellecour-assurances-fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.bellecour-assurances\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.bellecour-assurances.fr$1 [R=permanent,L]

    RewriteRule ^/pr(.+)sentation(.+)Bellecour(.+)v2\.pdf / [R=permanent,L]
    RewriteRule ^/Bellecour\.pdf / [R=permanent,L]
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName sbm-immo.com
    ServerAlias *.sbm-immo.com
    ErrorLog logs/sbm-immo-com-error_log
    CustomLog logs/sbm-immo-com-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.sbm-immo\.com$ [NC]
    RewriteRule ^(.*)$ http://www.sbm-immo.com$1 [R=permanent,L]
    RewriteRule ^/sites/sbm-immo.com/mobile(.*) /mobile$1 [R=permanent,L]
    RewriteRule	^/lagence/agence-immobiliere-annecy /lagence/notre-agence-annecy [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/sbm-immo.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName bar-leroyal.fr
    ServerAlias *.bar-leroyal.fr
    ErrorLog logs/bar-leroyal-error_log
    CustomLog logs/bar-leroyal-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.bar-leroyal\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.bar-leroyal.fr$1 [R=permanent,L]
#    RewriteRule ^/sites/bar-leroyal.fr/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/bar-leroyal.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName maisondugeneral.com
    ServerAlias *.maisondugeneral.com
    ErrorLog logs/maisondugeneral-error_log
    CustomLog logs/maisondugeneral-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.maisondugeneral\.com$ [NC]
    RewriteRule ^(.*)$ http://www.maisondugeneral.com$1 [R=permanent,L]
#    RewriteRule ^/sites/maisondugeneral.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/maisondugeneral.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName delphine-teillaud.com
    ServerAlias *.delphine-teillaud.com
    ErrorLog logs/delphine-teillaud-error_log
    CustomLog logs/delphine-teillaud-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.delphine-teillaud\.com$ [NC]
    RewriteRule ^(.*)$ http://www.delphine-teillaud.com$1 [R=permanent,L]
    RewriteRule ^/sites/delphine-teillaud.com/mobile(.*) /mobile$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/delphine-teillaud.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName lignhabitat.fr
    ServerAlias *.lignhabitat.fr
    ErrorLog logs/lignhabitat-error_log
    CustomLog logs/lignhabitat-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.lignhabitat\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.lignhabitat.fr$1 [R=permanent,L]
#    RewriteRule ^/sites/lignhabitat.fr/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/lignhabitat.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName maisonshamster.com
    ServerAlias *.maisonshamster.com
    ErrorLog logs/maisonhamster-error_log
    CustomLog logs/maisonshamster-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.maisonshamster\.com$ [NC]
    RewriteRule ^(.*)$ http://www.maisonshamster.com$1 [R=permanent,L]
#    RewriteRule ^/sites/maisonshamster.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/maisonshamster.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName noyal-poids-lourds.com
    ServerAlias *.noyal-poids-lourds.com
    ErrorLog logs/noyal-poids-lourds-error_log
    CustomLog logs/noyal-poids-lourds-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.noyal-poids-lourds\.com$ [NC]
    RewriteRule ^(.*)$ http://www.noyal-poids-lourds.com$1 [R=permanent,L]
#    RewriteRule ^/sites/noyal-poids-lourds.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/noyal-poids-lourds.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName tombelaine-coquillages.com
    ServerAlias *.tombelaine-coquillages.com
    ErrorLog logs/tombelaine-coquillages-error_log
    CustomLog logs/tombelaine-coquillages-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.tombelaine-coquillages\.com$ [NC]
    RewriteRule ^(.*)$ http://www.tombelaine-coquillages.com$1 [R=permanent,L]
#    RewriteRule ^/sites/tombelaine-coquillages.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/tombelaine-coquillages.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName natterip.com
    ServerAlias *.natterip.com natternyc.com *.natternyc.com
    ErrorLog logs/natterip-error_log
    CustomLog logs/natterip-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.natterip\.com$ [NC]
    RewriteRule ^(.*)$ http://www.natterip.com$1 [R=permanent,L]
#    RewriteRule ^/sites/natterip.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/natterip.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName binois-menuiserie.fr
    ServerAlias *.binois-menuiserie.fr
    ErrorLog logs/binois-menuiserie-error_log
    CustomLog logs/binois-menuiserie-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.binois-menuiserie\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.binois-menuiserie.fr$1 [R=permanent,L]
#    RewriteRule ^/sites/binois-menuiserie.fr/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/binois-menuiserie/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName prigent-associes.fr
    ServerAlias *.prigent-associes.fr
    ErrorLog logs/prigent-associes-error_log
    CustomLog logs/prigent-associes-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.prigent-associes\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.prigent-associes.fr$1 [R=permanent,L]
#    RewriteRule ^/sites/prigent-associes.fr/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/prigent-associes.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName cabinet-martin.fr
    ServerAlias *.cabinet-martin.fr
    ErrorLog logs/cabinet-martin-error_log
    CustomLog logs/cabinet-martin-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.cabinet-martin\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.cabinet-martin.fr$1 [R=permanent,L]
    RewriteRule ^/sites/martin-martin.fr/mobile(.*) /mobile$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/cabinet-martin.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName immobilier.orange.com
    ServerAlias *.immobilier.orange.com
    ErrorLog logs/immobilier.orange-error_log
    CustomLog logs/immobilier.orange-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} ^www\.immobilier.orange\.com$ [NC]
    RewriteRule ^(.*)$ http://immobilier.orange.com$1 [R=permanent,L]
#    RewriteRule ^/sites/immobilier.orange.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal730/sites/immobilier.orange.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName expressivetherapycenter.com
    ServerAlias *.expressivetherapycenter.com
    ErrorLog logs/expressivetherapycenter-error_log
    CustomLog logs/expressivetherapycenter-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.expressivetherapycenter\.com$ [NC]
    RewriteRule ^(.*)$ http://www.expressivetherapycenter.com$1 [R=permanent,L]
#    RewriteRule ^/sites/expressivetherapycenter.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/expressivetherapycenter.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName abita-immobilier.com
    ServerAlias *.abita-immobilier.com
    ErrorLog logs/abita-immobilier-error_log
    CustomLog logs/abita-immobolier-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.abita-immobilier\.com$ [NC]
    RewriteRule ^(.*)$ http://www.abita-immobilier.com$1 [R=permanent,L]
    RewriteRule ^/sites/abita-immobilier.com/mobile(.*) /mobile$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/abita-immobilier.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName cap-malo.com
    ServerAlias *.cap-malo.com
    ErrorLog logs/cap-malo-error_log
    CustomLog logs/cap-malo-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.cap-malo\.com$ [NC]
    RewriteRule ^(.*)$ http://www.cap-malo.com$1 [R=permanent,L]
#    RewriteRule ^/sites/cap-malo.com/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal726/sites/cap-malo.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName pjtoulemont.fr
    ServerAlias *.pjtoulemont.fr
    ErrorLog logs/pjtoulemont-error_log
    CustomLog logs/pjtoulemont-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.pjtoulemont\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.pjtoulemont.fr$1 [R=permanent,L]
#    RewriteRule ^/sites/pjtoulemont.fr/mobile(.*) /mobile$1 [R=permanent,L]

#    Alias /mobile ${DRUPAL_HOME}/drupal730/sites/pjtoulemont.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName 2a-immo.fr
    ServerAlias *.2a-immo.fr
    ErrorLog logs/2a-immo-error_log
    CustomLog logs/2a-immo-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.2a-immo\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.2a-immo.fr/$1 [R=permanent,L]
    RewriteRule ^/sites/2a-immo.fr/mobile(.*) /mobile$1 [R=permanent,L]

    Alias /mobile ${DRUPAL_HOME}/drupal730/sites/2a-immo.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName fauvelimmobilier.com
    ServerAlias *.fauvelimmobilier.com
    ErrorLog logs/fauvelimmobilier-error_log
    CustomLog logs/fauvelimmobilier-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.fauvelimmobilier\.com$ [NC]
    RewriteRule ^(.*)$ http://www.fauvelimmobilier.com$1 [R=permanent,L]

    RewriteRule ^/sites/fauvelimmobilier.com/mobile(.*) /mobile$1 [R=permanent,L]
    Alias /mobile ${DRUPAL_HOME}/drupal730/sites/fauvelimmobilier.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    #DocumentRoot ${DRUPAL_HOME}/drupal730
    DocumentRoot /var/www/html/maintenance
    ServerName wellcom.bzh
    ServerAlias *.wellcom.bzh
    ErrorLog logs/wellcom-error_log
    CustomLog logs/wellcom-access_log common

    RewriteEngine on
    #RewriteCond %{HTTP_HOST} !^www\.wellcom\.bzh$ [NC]
    #RewriteRule ^(.*)$ http://www.wellcom.bzh$1 [R=permanent,L]

    ErrorDocument 503 /index.html
    Header Set Cache-Control "max-age=0, no-store"

    #RewriteRule ^/sites/wellcom.bzh/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/wellcom.bzh/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName gaalon-guerlesquin.fr
    ServerAlias *.gaalon-guerlesquin.fr
    ErrorLog logs/gaalon-guerlesquin-error_log
    CustomLog logs/gaalon-guerlesquin-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.gaalon-guerlesquin\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.gaalon-guerlesquin.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/gaalon-guerlesquin.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/gaalon-guerlesquin.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName hotel-chateaubriand-st-malo.com
    ServerAlias *.hotel-chateaubriand-st-malo.com
    ErrorLog logs/hotel-chateaubriand-st-malo-error_log
    CustomLog logs/hotel-chateaubriand-st-malo-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.hotel-chateaubriand-st-malo\.com$ [NC]
    RewriteRule ^(.*)$ http://www.hotel-chateaubriand-st-malo.com$1 [R=permanent,L]

    #RewriteRule ^/sites/hotel-chateaubriand-st-malo.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal726/sites/hotel-chateaubriand-st-malo.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName hotel-cite-st-malo-bretagne.com
    ServerAlias *.hotel-cite-st-malo-bretagne.com
    ErrorLog logs/hotel-cite-st-malo-bretagne-error_log
    CustomLog logs/hotel-cite-st-malo-bretagne-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.hotel-cite-st-malo-bretagne\.com$ [NC]
    RewriteRule ^(.*)$ http://www.hotel-cite-st-malo-bretagne.com$1 [R=permanent,L]

    #RewriteRule ^/sites/hotel-cite-st-malo-bretagne.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal726/sites/hotel-cite-st-malo-bretagne.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName hotel-annedebretagne.com
    ServerAlias *.hotel-annedebretagne.com
    ErrorLog logs/hotel-annedebretagne-error_log
    CustomLog logs/hotel-annedebretagne-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.hotel-annedebretagne\.com$ [NC]
    RewriteRule ^(.*)$ http://www.hotel-annedebretagne.com$1 [R=permanent,L]

    #RewriteRule ^/sites/hotel-annedebretagne.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal726/sites/hotel-annedebretagne.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName le5-restaurant.com
    ServerAlias *.le5-restaurant.com
    ErrorLog logs/le5-restaurant-error_log
    CustomLog logs/le5-restaurant-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.le5-restaurant\.com$ [NC]
    RewriteRule ^(.*)$ http://www.le5-restaurant.com$1 [R=permanent,L]

    #RewriteRule ^/sites/le5-restaurant.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal726/sites/le5-restaurant.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal726/
    ServerName hotel-des-lices.com
    ServerAlias *.hotel-des-lices.com
    ErrorLog logs/hotel-des-lices-error_log
    CustomLog logs/hotel-lices-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.hotel-des-lices\.com$ [NC]
    RewriteRule ^(.*)$ http://www.hotel-des-lices.com$1 [R=permanent,L]

    #RewriteRule ^/sites/hotel-des-lices.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal726/sites/hotel-des-lices.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName potintp.com
    ServerAlias *.potintp.com
    ErrorLog logs/potintp-error_log
    CustomLog logs/potintp-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.potintp\.com$ [NC]
    RewriteRule ^(.*)$ http://www.potintp.com$1 [R=permanent,L]

    #RewriteRule ^/sites/potintp.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/potintp.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName schatzipark.fr
    ServerAlias *.schatzipark.fr
    ErrorLog logs/schatzipark-error_log
    CustomLog logs/schatzipark-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.schatzipark\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.schatzipark.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/schatzipark.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/schatzipark.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName hotel-mulhouse.com
    ServerAlias *.hotel-mulhouse.com
    ErrorLog logs/hotel-mulhouse-error_log
    CustomLog logs/hotel-mulhouse-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.hotel-mulhouse\.com$ [NC]
    RewriteRule ^(.*)$ http://www.hotel-mulhouse.com$1 [R=permanent,L]

    #RewriteRule ^/sites/hotel-mulhouse.com/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/hotel-mulhouse.com/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName am-rochereuil.fr
    ServerAlias *.am-rochereuil.fr
    ErrorLog logs/am-rochereuil-error_log
    CustomLog logs/am-rochereuil-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.am-rochereuil\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.am-rochereuil.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/am-rochereuil.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/am-rochereuil.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName francois-notaire-stmalo.fr
    ServerAlias *.francois-notaire-stmalo.fr
    ErrorLog logs/francois-notaire-stmalo-error_log
    CustomLog logs/francois-notaire-stmalo-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.francois-notaire-stmalo\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.francois-notaire-stmalo.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/francois-notaire-stmalo.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/francois-notaire-stmalo.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName chouzenoux-architecture.fr
    ServerAlias *.chouzenoux-architecture.fr
    ErrorLog logs/chouzenoux-architecture.fr-error_log
    CustomLog logs/chouzenoux-architecture.fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.chouzenoux-architecture\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.chouzenoux-architecture.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/chouzenoux-architecture.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/chouzenoux-architecture.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName rupin-plomberie.fr
    ServerAlias *.rupin-plomberie.fr
    ErrorLog logs/rupin-plomberie.fr-error_log
    CustomLog logs/rupin-plomberie.fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.rupin-plomberie\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.rupin-plomberie.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/itsinyou.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/rupin-plomberie.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName boucheriebriand.fr
    ServerAlias *.boucheriebriand.fr
    ErrorLog logs/boucheriebriand.fr-error_log
    CustomLog logs/boucheriebriand.fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.boucheriebriand\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.boucheriebriand.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/boucheriebriand.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/boucheriebriand.fr/mobile/
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin info@acreat.com
    DocumentRoot ${DRUPAL_HOME}/drupal730
    ServerName quatreb.fr
    ServerAlias *.quatreb.fr
    ErrorLog logs/quatreb.fr-error_log
    CustomLog logs/quatreb.fr-access_log common

    RewriteEngine on
    RewriteCond %{HTTP_HOST} !^www\.quatreb\.fr$ [NC]
    RewriteRule ^(.*)$ http://www.quatreb.fr$1 [R=permanent,L]

    #RewriteRule ^/sites/quatreb.fr/mobile(.*) /mobile$1 [R=permanent,L]
    #Alias /mobile ${DRUPAL_HOME}/drupal730/sites/quatreb.fr/mobile/
</VirtualHost>

Include conf-enabled/*.conf
Include sites-enabled/*.conf
